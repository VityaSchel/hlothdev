import { useSpring, animated } from '@react-spring/three'

export default function AnimatedMaterialPrimitive(props) {
  console.log(props.object);
  return (
    <animated.group>

    </animated.group>
  )
}
